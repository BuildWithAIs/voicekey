# Voice Key Website

Astro static marketing site for Voice Key. It builds for GitHub Pages under the
`/voicekey` base path and publishes English plus Chinese routes.

## Commands

```bash
npm install
npm run dev
npm run build
npm run preview
```

The version badge reads the root `package.json` version at build time, then may
refresh in the browser from the latest GitHub release.
