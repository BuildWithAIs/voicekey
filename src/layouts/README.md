# layouts/

应用布局容器目录，定义应用的整体结构和导航框架。

## 文件

### `MainLayout.tsx`

应用主布局组件，提供：

- **顶部拖拽区域** - macOS 窗口拖动支持（仅 macOS）
- **左侧边栏** - 包含应用 Logo、导航菜单（Home, Settings, History）和版本信息
- **右侧内容区** - 渲染当前路由对应的页面内容
- **路由导航** - 基于 hash 路由的页面切换逻辑

所有页面组件通过 `MainLayout` 包裹，确保统一的视觉和交互体验。
